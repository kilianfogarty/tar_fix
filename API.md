Kimberlyn look here for API to call the database.

## GET /api/reports
Returns the 50 newest reports.

## POST /api/reports
Accepts data with:
- image (file)
- decription (string)

Returns the created report object.